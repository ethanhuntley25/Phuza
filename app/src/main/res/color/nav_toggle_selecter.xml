<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is the color that will show for the pill background when selected -->
    <item android:color="@color/aquamarine" android:state_checked="true" />
    <!-- This ensures no background shows when not selected -->
    <item android:color="@android:color/transparent" />
</selector>

